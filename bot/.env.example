# Copia a ".env" (NUNCA lo subas). Los secretos van aquí, jamás en el código.

# A dónde manda los reportes la skill (tu backend de server/).
# En local: http://localhost:3001 · En la nube: la URL https de tu host.
BACKEND_URL=http://localhost:3001

# Token del bot de Telegram (lo da @BotFather). NO lo pegues en el chat ni en git.
# TELEGRAM_BOT_TOKEN=
